---
- name: Import vagrant machines
  hosts: localhost
  become: true

  tasks:
    - name: 6_import_vagrant_machines... Create dir nvim under /home/lvazquez/.config/
      ansible.builtin.file:
        path: /home/lvazquez/vagrant
        state: directory
        mode: '0755'
        owner: lvazquez
        group: lvazquez